version: "3.9"

services:
  gadm-downloader:
    build: .
    image: gadm-downloader:latest
    container_name: gadm-downloader

    volumes:
      - ./output:/app/output
      - ./logs:/app/logs

    command: ["python", "gadm_downloader/main.py"]

    restart: "no"